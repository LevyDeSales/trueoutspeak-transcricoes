{
  "name": "trueoutspeak-transcricoes",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "export": "node scripts/export.mjs",
    "sync": "node scripts/sync.mjs",
    "sync:check": "node scripts/sync.mjs --check",
    "test": "node --test",
    "verify": "node scripts/verify.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
